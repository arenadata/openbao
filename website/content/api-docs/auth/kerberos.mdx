---
sidebar_label: Kerberos
description: This is the API documentation for the OpenBao Kerberos auth method plugin.
---

# Kerberos auth method (API)

This is the API documentation for the OpenBao Kerberos auth method plugin. To
learn more about the usage and operation, see the
[OpenBao Kerberos auth method](/docs/auth/kerberos).

This documentation assumes the Kerberos auth method is mounted at the
`auth/kerberos` path in OpenBao. Since it is possible to enable auth methods at
any location, please update your API calls accordingly.

The Kerberos auth method validates the SPNEGO token and then resolves
policies either through LDAP group membership (`config/ldap` plus `groups/`)
or, when `config/ldap` is not set, through [roles](#roles) bound to Kerberos
principals. One of the two has to be configured.

## Configure OpenBao Kerberos

This endpoint configures the keytab and service account to be used by OpenBao
for verifying inbound SPNEGO tokens.

| Method | Path                    |
| :----- | :---------------------- |
| `POST` | `/auth/kerberos/config` |

- `keytab` `(string: <required>)` – A base 64 representation of the contents
  of the Kerberos keytab that will be used for verifying inbound SPNEGO tokens.
  It should contain an entry matching the service account given. This can be
  created through the following command: `$ base64 openbao.keytab > openbao.keytab.base64`.
- `service_account` `(string: "")` – The keytab entry used to decrypt every
  service ticket, for keytabs whose entry name differs from the service
  principal clients request (an Active Directory service account such as
  `"openbao_svc"`). When empty, each ticket is decrypted with the entry named
  by its own service principal, so one keytab can hold an entry per node of an
  HA cluster. With LDAP configured it is also the LDAP service account created
  for OpenBao.
- `remove_instance_name` - Removes any instance names from a Kerberos service
  principal name when parsing the keytab file. For example when this is set to true,
  if a keytab has the service principal name `foo/localhost@example.com`, OpenBao
  will strip the service principal name to just be `foo@example.com`.
- `add_group_aliases` - When set to true, OpenBao will add any LDAP groups found for
  the user as group aliases.

### Sample request

```shell-session
$ curl \
    --header "X-Vault-Token: ..." \
    --request POST \
    --data @payload.json \
    http://127.0.0.1:8200/v1/auth/kerberos/config
```

### Sample payload

```json
{
  "add_group_aliases": false,
  "keytab": "BQIAAAA6AAEACk1BVFJJWC5MQU4ACXZhdWx0X3N2YwAAAAFdzZSjAgAXABDwhEyRR9nRqkpP8KTn2A83AAAAAg==",
  "remove_instance_name": false,
  "service_account": "openbao_svc"
}
```

## Read OpenBao Kerberos

This endpoint retrieves the service account for the Kerberos auth method.
The keytab is not returned because it is sensitive information.

| Method | Path                    |
| :----- | :---------------------- |
| `GET`  | `/auth/kerberos/config` |

### Sample request

```shell-session
$ curl \
    --header "X-Vault-Token: ..." \
    http://127.0.0.1:8200/v1/auth/kerberos/config
```

### Sample response

```json
{
  "auth": null,
  "warnings": null,
  "wrap_info": null,
  "data": {
    "add_group_aliases": false,
    "remove_instance_name": false,
    "service_account": "openbao_svc"
  },
  "lease_duration": 0,
  "renewable": false,
  "lease_id": ""
}
```

## Configure Kerberos LDAP

This endpoint configures LDAP in the Kerberos auth method.

| Method | Path                         |
| :----- | :--------------------------- |
| `POST` | `/auth/kerberos/config/ldap` |

### Parameters

- `url` `(string: <required>)` – The LDAP server to connect to. Examples:
  `ldap://ldap.myorg.com`, `ldaps://ldap.myorg.com:636`. Multiple URLs can be
  specified with commas, e.g. `ldap://ldap.myorg.com,ldap://ldap2.myorg.com`;
  these will be tried in-order.
- `case_sensitive_names` `(bool: false)` – If set, user and group names
  assigned to policies within the backend will be case sensitive. Otherwise,
  names will be normalized to lower case. Case will still be preserved when
  sending the username to the LDAP server at login time; this is only for
  matching local user/group definitions.
- `starttls` `(bool: false)` – If true, issues a `StartTLS` command after
  establishing an unencrypted connection.
- `tls_min_version` `(string: tls12)` – Minimum TLS version to use. Accepted
  values are `tls10`, `tls11`, `tls12` or `tls13`.
- `tls_max_version` `(string: tls12)` – Maximum TLS version to use. Accepted
  values are `tls10`, `tls11`, `tls12` or `tls13`.
- `insecure_tls` `(bool: false)` – If true, skips LDAP server SSL certificate
  verification - insecure, use with caution!
- `certificate` `(string: "")` – CA certificate to use when verifying LDAP server
  certificate, must be x509 PEM encoded.
- `binddn` `(string: "")` – Distinguished name of object to bind when performing
  user search. Example: `cn=openbao,ou=Users,dc=example,dc=com`
- `bindpass` `(string: "")` – Password to use along with `binddn` when performing
  user search.
- `userdn` `(string: "")` – Base DN under which to perform user search. Example:
  `ou=Users,dc=example,dc=com`
- `userattr` `(string: "")` – Attribute on user attribute object matching the
  username passed when authenticating. Examples: `sAMAccountName`, `cn`, `uid`
- `discoverdn` `(bool: false)` – Use anonymous bind to discover the bind DN of a
  user.
- `deny_null_bind` `(bool: true)` – This option prevents users from bypassing
  authentication when providing an empty password.
- `upndomain` `(string: "")` – The _userPrincipalDomain_ used to construct the UPN
  string for the authenticating user. The constructed UPN will appear as
  `[username]@UPNDomain`. Example: `example.com`, which will cause openbao to bind
  as `username@example.com`.
- `groupfilter` `(string: "")` – Go template used when constructing the group
  membership query. The template can access the following context variables:
  \[`UserDN`, `Username`\]. The default is
  `(|(memberUid={{.Username}})(member={{.UserDN}})(uniqueMember={{.UserDN}}))`,
  which is compatible with several common directory schemas. To support
  nested group resolution for Active Directory, instead use the following
  query: `(&(objectClass=group)(member:1.2.840.113556.1.4.1941:={{.UserDN}}))`.
- `groupdn` `(string: "")` – LDAP search base to use for group membership
  search. This can be the root containing either groups or users. Example:
  `ou=Groups,dc=example,dc=com`
- `groupattr` `(string: "")` – LDAP attribute to follow on objects returned by
  `groupfilter` in order to enumerate user group membership. Examples: for
  groupfilter queries returning _group_ objects, use: `cn`. For queries
  returning _user_ objects, use: `memberOf`. The default is `cn`.

@include 'tokenfields.mdx'

### Sample request

```shell-session
$ curl \
    --header "X-Vault-Token: ..." \
    --request POST \
    --data @payload.json \
    http://127.0.0.1:8200/v1/auth/kerberos/config/ldap
```

### Sample payload

```json
{
  "binddn": "cn=openbao,ou=Users,dc=example,dc=com",
  "deny_null_bind": true,
  "discoverdn": false,
  "groupattr": "cn",
  "groupdn": "ou=Groups,dc=example,dc=com",
  "groupfilter": "(\u0026(objectClass=group)(member:1.2.840.113556.1.4.1941:={{.UserDN}}))",
  "insecure_tls": false,
  "starttls": false,
  "tls_max_version": "tls12",
  "tls_min_version": "tls12",
  "url": "ldaps://ldap.myorg.com:636",
  "userattr": "samaccountname",
  "userdn": "ou=Users,dc=example,dc=com"
}
```

## Read Kerberos LDAP configuration

This endpoint retrieves the LDAP configuration for the Kerberos auth method.

| Method | Path                         |
| :----- | :--------------------------- |
| `GET`  | `/auth/kerberos/config/ldap` |

### Sample request

```shell-session
$ curl \
    --header "X-Vault-Token: ..." \
    http://127.0.0.1:8200/v1/auth/kerberos/config/ldap
```

### Sample response

```json
{
  "auth": null,
  "warnings": null,
  "wrap_info": null,
  "data": {
    "binddn": "cn=openbao,ou=Users,dc=example,dc=com",
    "bindpass": "",
    "certificate": "",
    "deny_null_bind": true,
    "discoverdn": false,
    "groupattr": "cn",
    "groupdn": "ou=Groups,dc=example,dc=com",
    "groupfilter": "(\u0026(objectClass=group)(member:1.2.840.113556.1.4.1941:={{.UserDN}}))",
    "insecure_tls": false,
    "starttls": false,
    "tls_max_version": "tls12",
    "tls_min_version": "tls12",
    "upndomain": "",
    "url": "ldaps://ldap.myorg.com:636",
    "userattr": "samaccountname",
    "userdn": "ou=Users,dc=example,dc=com"
  },
  "lease_duration": 0,
  "renewable": false,
  "lease_id": ""
}
```

## Delete Kerberos LDAP configuration

This endpoint removes the LDAP configuration. Logins then resolve policies
through [roles](#roles).

| Method   | Path                         |
| :------- | :--------------------------- |
| `DELETE` | `/auth/kerberos/config/ldap` |

### Sample request

```shell-session
$ curl \
    --header "X-Vault-Token: ..." \
    --request DELETE \
    http://127.0.0.1:8200/v1/auth/kerberos/config/ldap
```

## List Kerberos LDAP groups

This endpoint returns a list of existing LDAP groups in the Kerberos auth method.

| Method | Path                    |
| :----- | :---------------------- |
| `LIST` | `/auth/kerberos/groups` |

### Parameters

 - `after` `(string: "")` - Optional entry to begin listing after for
   pagination; not required to exist.

 - `limit` `(int: 0)` - Optional number of entries to return; defaults
   to all entries.

### Sample request

```shell-session
$ curl \
    --header "X-Vault-Token: ..." \
    --request LIST \
    http://127.0.0.1:8200/v1/auth/kerberos/groups
```

### Sample response

```json
{
  "auth": null,
  "warnings": null,
  "wrap_info": null,
  "data": {
    "keys": ["scientists", "engineers"]
  },
  "lease_duration": 0,
  "renewable": false,
  "lease_id": ""
}
```

## Read Kerberos LDAP group

This endpoint returns the policies associated with a Kerberos LDAP group.

| Method | Path                          |
| :----- | :---------------------------- |
| `GET`  | `/auth/kerberos/groups/:name` |

### Parameters

- `name` `(string: <required>)` – The name of the LDAP group.

### Sample request

```shell-session
$ curl \
    --header "X-Vault-Token: ..." \
    http://127.0.0.1:8200/v1/auth/kerberos/groups/admins
```

### Sample response

```json
{
  "data": {
    "policies": ["admin", "default"]
  },
  "renewable": false,
  "lease_id": "",
  "lease_duration": 0,
  "warnings": null
}
```

## Create/Update Kerberos LDAP group

This endpoint creates or updates LDAP group policies.

| Method | Path                          |
| :----- | :---------------------------- |
| `POST` | `/auth/kerberos/groups/:name` |

### Parameters

- `name` `(string: <required>)` – The name of the LDAP group.
- `policies` `(string: "")` – Comma-separated list of policies associated to the
  group.

### Sample payload

```json
{
  "policies": "admin,default"
}
```

### Sample request

```shell-session
$ curl \
    --header "X-Vault-Token: ..." \
    --request POST \
    --data @payload.json \
    http://127.0.0.1:8200/v1/auth/kerberos/groups/admins
```

## Delete Kerberos LDAP group

This endpoint deletes the LDAP group and policy association.

| Method   | Path                          |
| :------- | :---------------------------- |
| `DELETE` | `/auth/kerberos/groups/:name` |

### Parameters

- `name` `(string: <required>)` – The name of the LDAP group.

### Sample request

```shell-session
$ curl \
    --header "X-Vault-Token: ..." \
    --request DELETE \
    http://127.0.0.1:8200/v1/auth/kerberos/groups/admins
```

## Roles

Roles bind Kerberos principals to token parameters without LDAP. They are
consulted only while `config/ldap` is not set. At login exactly one role must
match the authenticated principal; see [Login with Kerberos](#login-with-kerberos).

### List roles

| Method | Path                   |
| :----- | :--------------------- |
| `LIST` | `/auth/kerberos/roles` |

#### Parameters

 - `after` `(string: "")` - Optional entry to begin listing after for
   pagination; not required to exist.

 - `limit` `(int: 0)` - Optional number of entries to return; defaults
   to all entries.

#### Sample request

```shell-session
$ curl \
    --header "X-Vault-Token: ..." \
    --request LIST \
    http://127.0.0.1:8200/v1/auth/kerberos/roles
```

#### Sample response

```json
{
  "data": {
    "keys": ["hadoop", "operators"]
  }
}
```

### Read role

| Method | Path                         |
| :----- | :--------------------------- |
| `GET`  | `/auth/kerberos/roles/:name` |

#### Parameters

- `name` `(string: <required>)` – The name of the role.

#### Sample request

```shell-session
$ curl \
    --header "X-Vault-Token: ..." \
    http://127.0.0.1:8200/v1/auth/kerberos/roles/hadoop
```

#### Sample response

```json
{
  "data": {
    "bound_principals": ["hadoop/*@MATRIX.LAN", "nn/*@MATRIX.LAN"],
    "token_bound_cidrs": [],
    "token_explicit_max_ttl": 0,
    "token_max_ttl": 0,
    "token_no_default_policy": false,
    "token_num_uses": 0,
    "token_period": 0,
    "token_policies": ["hadoop-keys"],
    "token_strictly_bind_ip": false,
    "token_ttl": 3600,
    "token_type": "default"
  }
}
```

### Create/Update role

| Method | Path                         |
| :----- | :--------------------------- |
| `POST` | `/auth/kerberos/roles/:name` |

#### Parameters

- `name` `(string: <required>)` – The name of the role.
- `bound_principals` `(array: <required>)` – Patterns matched against the
  full principal name `primary/instance@REALM`. `*` matches any characters;
  the rest is compared literally and case-sensitively. Any match is sufficient.
  Empty entries are dropped.

@include 'tokenfields.mdx'

Only the `token_*` parameter names are accepted; the deprecated `policies`
alias is not. `token_period` may not exceed the mount's maximum lease TTL.

#### Sample payload

```json
{
  "bound_principals": "hadoop/*@MATRIX.LAN,nn/*@MATRIX.LAN",
  "token_policies": "hadoop-keys",
  "token_ttl": "1h"
}
```

#### Sample request

```shell-session
$ curl \
    --header "X-Vault-Token: ..." \
    --request POST \
    --data @payload.json \
    http://127.0.0.1:8200/v1/auth/kerberos/roles/hadoop
```

### Delete role

| Method   | Path                         |
| :------- | :--------------------------- |
| `DELETE` | `/auth/kerberos/roles/:name` |

#### Parameters

- `name` `(string: <required>)` – The name of the role.

#### Sample request

```shell-session
$ curl \
    --header "X-Vault-Token: ..." \
    --request DELETE \
    http://127.0.0.1:8200/v1/auth/kerberos/roles/hadoop
```

## Login with Kerberos

This endpoint allows you to log in with a valid Kerberos SPNEGO
token. This token is obtained by the client, marshalled, and
converted to base 64 using standard encoding.

Example SPNEGO token (newlines added for readability):

```

sWw
```

| Method | Path                   |
| :----- | :--------------------- |
| `POST` | `/auth/kerberos/login` |

### Parameters

- `role` `(string: "")` – Optional. Accepted only when policies are resolved
  through roles (no `config/ldap`); otherwise the request is rejected.
  Restricts the login to the named role. Without it every role is matched and
  exactly one must match; no match, or several matches, returns a 403 naming
  the candidate roles. The response reports the role used in the `role`
  metadata field and the entity alias is the full principal
  `primary/instance@REALM`. The role's `token_bound_cidrs` are enforced at
  login. Tokens issued from roles are renewable; tokens issued through LDAP
  are not.

- `delegation_token` `(string: "")` – A delegation token issued by
  `delegation/token`, in its URL string form. Logs in as the token's owner
  with the role recorded at issuance; no `Authorization` header is needed
  and `role` is rejected. The OpenBao token's explicit max TTL is capped by
  the delegation token's remaining time to `expiry`, and it renews only
  while the delegation token is valid. For a token issued with `doas`, the
  `real_user` metadata field names the principal that issued it, and the
  login and each renewal require that a [proxy](#createupdate-proxy) still
  lets that principal impersonate the owner. See
  [Delegation tokens](#delegation-tokens).

### Sample request

```shell-session
$ curl \
    --header "Authorization: Negotiate YIIFSw...sWw" \
    --request POST \
    http://127.0.0.1:8200/v1/auth/kerberos/login
```

## Delegation tokens

Delegation tokens let a SPNEGO-authenticated principal hand a credential to
processes without Kerberos credentials. They are byte-compatible with Hadoop's
`AbstractDelegationTokenIdentifier` and `Token`, require roles (no
`config/ldap`) and are enabled by writing `config/delegation`. The
`delegation/` endpoints take the same `Authorization: Negotiate` header as
`login`; a delegation token can never be used to obtain another one. Dates in
responses are milliseconds since the Unix epoch, as in Hadoop.

### Configure delegation tokens

| Method | Path                                |
| :----- | :---------------------------------- |
| `POST` | `/auth/kerberos/config/delegation`  |

Writing the configuration enables delegation tokens. Parameters not sent keep
their current value, or their default on the first write.

#### Parameters

- `renew_interval` `(duration: "24h")` – How far a renewal extends a token's
  `expiry`, capped by its `max_date`. Also the initial expiry of a new token.
  Must not exceed `max_lifetime`.
- `max_lifetime` `(duration: "168h")` – Hard lifetime ceiling of an issued
  token; renewals never extend a token past it.
- `key_rotation_interval` `(duration: "24h")` – How often the signing key is
  replaced. A key is kept while a token signed with it exists.
- `cleanup_interval` `(duration: "1h")` – How often expired tokens and
  unreferenced signing keys are removed from storage.
- `token_kind` `(string: "OPENBAO_DELEGATION_TOKEN")` – Hadoop token kind
  written into issued tokens and required on presented ones. Must match the
  kind of the `TokenIdentifier` class on the Hadoop side.
- `doas_realm` `(string: "")` – Realm of a `doas` name given without one,
  such as the Hadoop short name of a user from a trusted realm. Empty means
  the caller's realm.

#### Sample payload

```json
{
  "renew_interval": "24h",
  "max_lifetime": "168h"
}
```

#### Sample request

```shell-session
$ curl \
    --header "X-Vault-Token: ..." \
    --request POST \
    --data @payload.json \
    http://127.0.0.1:8200/v1/auth/kerberos/config/delegation
```

### Read delegation token configuration

| Method | Path                                |
| :----- | :---------------------------------- |
| `GET`  | `/auth/kerberos/config/delegation`  |

#### Sample request

```shell-session
$ curl \
    --header "X-Vault-Token: ..." \
    http://127.0.0.1:8200/v1/auth/kerberos/config/delegation
```

#### Sample response

```json
{
  "data": {
    "cleanup_interval": 3600,
    "doas_realm": "",
    "key_rotation_interval": 86400,
    "max_lifetime": 604800,
    "renew_interval": 86400,
    "token_kind": "OPENBAO_DELEGATION_TOKEN"
  }
}
```

### Delete delegation token configuration

| Method   | Path                                |
| :------- | :---------------------------------- |
| `DELETE` | `/auth/kerberos/config/delegation`  |

Disables delegation tokens and removes every issued token and signing key.

#### Sample request

```shell-session
$ curl \
    --header "X-Vault-Token: ..." \
    --request DELETE \
    http://127.0.0.1:8200/v1/auth/kerberos/config/delegation
```

### Issue delegation token

| Method | Path                              |
| :----- | :-------------------------------- |
| `POST` | `/auth/kerberos/delegation/token` |

Issues a token owned by the SPNEGO-authenticated principal or, with `doas`,
by the principal it names. The owner is matched against roles exactly as at
login and the selected role is recorded in the token; a login with the token
uses that role. The role's `token_bound_cidrs` are enforced on the caller.

With `doas` the caller acts as a Hadoop proxy user: the token's owner is the
named principal and its real user (`realUser` in the Hadoop identifier) is
the caller. A [proxy](#createupdate-proxy) must bind the caller, allow the
owner and admit the caller's address; otherwise the request is denied before
the owner's roles are consulted. The caller needs no role. Logins and
renewals check that a proxy still lets the caller impersonate the owner.

A server without `doas` support ignores the parameter with a warning and
issues a token owned by the caller, so a client should check `owner` and
`real_user` in the response.

#### Parameters

- `renewer` `(string: "")` – Name allowed to renew the token, compared with
  the renewing principal's full name and, when that principal is in the
  realm of the one issuing the token, with its `primary/instance` and its
  primary alone. At most 1024 bytes, without spaces. Empty makes the token
  non-renewable.
- `service` `(string: "")` – Hadoop token service written into the token,
  at most 1024 bytes. Not interpreted by OpenBao.
- `max_lifetime` `(duration: "")` – Requested hard lifetime, capped by the
  configured `max_lifetime`.
- `role` `(string: "")` – Role to issue the token for. Without it every role
  is matched and exactly one must match the owner.
- `doas` `(string: "")` – Principal to issue the token for instead of the
  caller. A name without a realm, such as the short name of a Hadoop proxy
  user, is in the `doas_realm` of the delegation token configuration or,
  without one, in the caller's realm; otherwise the realm follows the last
  `@`. Spaces, non-printing characters, `*` and `,` are rejected. Naming the
  caller itself issues an ordinary token.

#### Sample request

```shell-session
$ curl \
    --header "Authorization: Negotiate YIIFSw...sWw" \
    --request POST \
    --data '{"renewer": "yarn", "service": "openbao.example.com:8200"}' \
    http://127.0.0.1:8200/v1/auth/kerberos/delegation/token
```

#### Sample response

```json
{
  "data": {
    "expiry": 1756425600000,
    "issue_date": 1756339200000,
    "kind": "OPENBAO_DELEGATION_TOKEN",
    "master_key_id": 1,
    "max_date": 1756944000000,
    "owner": "hadoop/nn1.matrix.lan@MATRIX.LAN",
    "real_user": "",
    "renewer": "yarn",
    "role": "hadoop",
    "sequence_number": 42,
    "service": "openbao.example.com:8200",
    "token": "KAARYWxpY2VARVhBTVBMRS5DT00DYm9iAIoBmO35YACKAZkSBeQAKgEgAQ..."
  }
}
```

`token` is the Hadoop URL string form of `Token`, readable with
`Token.decodeFromUrlString()`; it contains the identifier, the password, the
kind and the service. `real_user` is empty unless the token was issued with
`doas`.

### Renew delegation token

| Method | Path                              |
| :----- | :-------------------------------- |
| `POST` | `/auth/kerberos/delegation/renew` |

Extends the token's `expiry` by `renew_interval`, never past `max_date`. The
SPNEGO-authenticated caller must be the token's renewer. A token issued with
`doas` renews only while a proxy still lets its real user impersonate its
owner.

#### Parameters

- `token` `(string: <required>)` – The delegation token in its URL string
  form.

#### Sample request

```shell-session
$ curl \
    --header "Authorization: Negotiate YIIFSw...sWw" \
    --request POST \
    --data '{"token": "KAARYWxpY2..."}' \
    http://127.0.0.1:8200/v1/auth/kerberos/delegation/renew
```

#### Sample response

```json
{
  "data": {
    "expiry": 1756512000000,
    "max_date": 1756944000000,
    "sequence_number": 42
  }
}
```

### Cancel delegation token

| Method | Path                               |
| :----- | :--------------------------------- |
| `POST` | `/auth/kerberos/delegation/cancel` |

Removes the token; it can no longer log in or be renewed. The
SPNEGO-authenticated caller must be the token's owner, its renewer, or a
principal that a proxy lets impersonate the owner from the caller's
address. OpenBao tokens already issued from it are not revoked but cannot
be renewed.

#### Parameters

- `token` `(string: <required>)` – The delegation token in its URL string
  form.

#### Sample request

```shell-session
$ curl \
    --header "Authorization: Negotiate YIIFSw...sWw" \
    --request POST \
    --data '{"token": "KAARYWxpY2..."}' \
    http://127.0.0.1:8200/v1/auth/kerberos/delegation/cancel
```

### List proxies

| Method | Path                   |
| :----- | :--------------------- |
| `LIST` | `/auth/kerberos/proxy` |

#### Parameters

- `after` `(string: "")` - Optional entry to begin listing after for
  pagination; not required to exist.

- `limit` `(int: 0)` - Optional number of entries to return; defaults
  to all entries.

#### Sample request

```shell-session
$ curl \
    --header "X-Vault-Token: ..." \
    --request LIST \
    http://127.0.0.1:8200/v1/auth/kerberos/proxy
```

#### Sample response

```json
{
  "data": {
    "keys": ["hive", "oozie"]
  }
}
```

### Read proxy

| Method | Path                         |
| :----- | :--------------------------- |
| `GET`  | `/auth/kerberos/proxy/:name` |

#### Parameters

- `name` `(string: <required>)` – The name of the proxy.

#### Sample request

```shell-session
$ curl \
    --header "X-Vault-Token: ..." \
    http://127.0.0.1:8200/v1/auth/kerberos/proxy/hive
```

#### Sample response

```json
{
  "data": {
    "allowed_principals": ["*@AD.MATRIX.LAN"],
    "bound_cidrs": ["10.0.12.0/24"],
    "bound_principals": ["hive/*@MATRIX.LAN"]
  }
}
```

### Create/Update proxy

| Method | Path                         |
| :----- | :--------------------------- |
| `POST` | `/auth/kerberos/proxy/:name` |

A proxy lets principals issue delegation tokens owned by other principals
with `doas`, the counterpart of `hadoop.proxyuser.<name>.users` and
`.hosts`; `.groups` has none. A request is granted when one proxy matches
the caller in `bound_principals`, the owner in `allowed_principals` and the
caller's address in `bound_cidrs`. Proxies are independent of roles.
Parameters not sent keep their current value; unknown parameters are
rejected.

#### Parameters

- `name` `(string: <required>)` – The name of the proxy.
- `bound_principals` `(array: <required>)` – Patterns matched against the
  full principal name of the caller. Each entry needs a realm after its last
  `@`, `*` for any realm; `*` is the only wildcard and also matches `/` and
  `@`. Empty entries are dropped.
- `allowed_principals` `(array: <required>)` – Patterns matched against the
  full principal name of the token owner, written like `bound_principals`.
  Empty entries are dropped.
- `bound_cidrs` `(array: [])` – IP addresses or CIDR blocks the caller must
  connect from. Host names, ports and IPv4-mapped IPv6 addresses are not
  accepted. Empty allows any address, whereas Hadoop refuses impersonation
  while `.hosts` is unset.

#### Sample payload

```json
{
  "bound_principals": "hive/*@MATRIX.LAN",
  "allowed_principals": "*@AD.MATRIX.LAN",
  "bound_cidrs": "10.0.12.0/24"
}
```

#### Sample request

```shell-session
$ curl \
    --header "X-Vault-Token: ..." \
    --request POST \
    --data @payload.json \
    http://127.0.0.1:8200/v1/auth/kerberos/proxy/hive
```

### Delete proxy

| Method   | Path                         |
| :------- | :--------------------------- |
| `DELETE` | `/auth/kerberos/proxy/:name` |

Tokens issued through the proxy stop logging in and renewing unless another
proxy allows them, whatever its `bound_cidrs`.

#### Parameters

- `name` `(string: <required>)` – The name of the proxy.

#### Sample request

```shell-session
$ curl \
    --header "X-Vault-Token: ..." \
    --request DELETE \
    http://127.0.0.1:8200/v1/auth/kerberos/proxy/hive
```
